import UIKit

final class ShowRouter
{
    private var controller: ShowViewController?

    func setRootController(controller: ShowViewController) {
        self.controller = controller
    }

    func next() {
    }
}
